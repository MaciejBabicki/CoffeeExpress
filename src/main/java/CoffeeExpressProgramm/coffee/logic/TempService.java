package CoffeeExpressProgramm.coffee.logic;

import org.springframework.stereotype.Service;

@Service
class TempService {
//    @Autowired
//        List<String> temp (CoffeeGroupRepository repo){
//        return repo.findAll().stream()
//                .flatMap(coffeeGroup -> coffeeGroup.getCoffee.stream())
//                .map(Coffee::getName)
//                .collect(Collectors.toList());

//    }
}
